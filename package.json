{
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "node --check cloudfunctions/api/index.js && for f in cloudfunctions/api/lib/*.js cloudfunctions/payNotify/index.js cloudfunctions/refundNotify/index.js cloudfunctions/platformNotify/index.js; do node --check \"$f\"; done"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
